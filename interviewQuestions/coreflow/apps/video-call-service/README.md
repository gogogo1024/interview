# video-call-service

项目一：实时 AI 视频通话服务。

开发与运行（ESM）：

```bash
cd apps/video-call-service
pnpm install
pnpm run dev

# 构建与运行
pnpm run build
pnpm run start
```

说明：本包使用 `type: "module"`（ESM），开发时通过 `ts-node/esm` 直接运行 TypeScript。
